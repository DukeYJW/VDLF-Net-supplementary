# CIFAR-100 (Experiment 1 — legacy tree)

Place `cifar-100-python` under this directory (same layout as `exp1codes/data/README.md`).

This matches the notebook path `data/cifar-100-python` when the working directory is `experiment_1`.
