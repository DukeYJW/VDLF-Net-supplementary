# Ablation checkpoints

Trained weights `best_ablation_*.pth` are **not** stored in GitHub (size limits).

Place them here after downloading from the Dropbox link in the repository root `README.md`, or retrain with `ablation_train.py` / `ablation_suite.ipynb`.
