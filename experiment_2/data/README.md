# Mini-ImageNet (Experiment 2 + ablation)

Place the prepared dataset so that **from `experiment_2/`** you have:

`data/mini-imagenet/`

with splits such as `train/`, `val/`, `test/` (see `experiment_2/check_mini_imagenet.py` and `experiment_2/README.md`).

Ablation code expects the same tree via `../experiment_2/data/mini-imagenet` when run from `experiment_ablation/`.
